import { Link } from 'react-router-dom';
import Auth from '../utils/auth'
import Sidebar from '../components/Sidebar'

const Home = () => {
    return (
        <><h1>home</h1>
            <Sidebar />
        </>

    );
};

export default Home;
